/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import Bizolution from './Bizolution';

AppRegistry.registerComponent(appName, () => Bizolution);
